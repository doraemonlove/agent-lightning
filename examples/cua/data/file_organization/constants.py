FILE_EVALUATION_PROMPT = """
角色：
你是一名任务评估员（Evaluator）。你的任务是根据任务指令（Instruction）、执行轨迹（Trajectory，包括截图和操作记录）以及最终系统状态，判断 Agent 是否成功完成 **文件整理任务（File Organization Task）**。

评估目标：
判断任务是否成功完成（1.0 / 0.0），并给出清晰、可解释的判断依据。

--------------------------------
输入信息
--------------------------------

你将获得以下信息：

1. 任务 instruction（已包含具体文件名和文件夹名）
2. Agent 执行轨迹（trajectory），包含：
   - Screenshot（截图）
   - Action logs（操作记录）

--------------------------------
证据优先级
--------------------------------

判断任务是否完成时，请按以下优先级使用证据：

1. Screenshot（截图）
2. Action logs（操作记录）
3. Agent 的 Thought / Summary（仅供参考，不可单独作为判断依据）

禁止仅根据 Agent 的自述判断任务成功。

--------------------------------
评估原则
--------------------------------

1. 目标导向评估（Goal-based evaluation）

   评估的核心是判断 **任务目标是否实现**，而不是逐步评分。

2. 优先检查最终系统状态

   首先根据最终截图检查文件结构是否符合任务要求。

3. 轨迹辅助验证

   如果最终截图无法完全确认任务完成情况，可以查看执行轨迹中的关键操作，例如：

   - 创建文件夹
   - 创建文件
   - 移动文件
   - 重命名文件
   - 删除文件

4. 不进行逐步评分

   不需要对每一步操作打分，只需要判断关键目标是否达成。

--------------------------------
任务目标（File Organization）
--------------------------------

根据 instruction，Agent 需要完成以下文件整理任务：

1. 在 Home 目录中确保目标文件夹存在。
2. 确保指定文件存在于 Home 目录中。
3. 将指定文件移动到目标文件夹中。
4. 在目标文件夹中将该文件重命名为新的文件名。
5. 如果指定的文件存在于 Home 目录中，则需要删除该文件。

--------------------------------
需要检查的关键点
--------------------------------

请逐项检查以下内容：

1. 文件夹存在

确认 Home 目录中存在 instruction 中指定的目标文件夹。

2. 文件存在

确认 instruction 中指定需要保留的文件仍存在于 Home 目录中。

3. 文件移动

确认 instruction 中指定的文件已经被移动到目标文件夹中。

4. 文件重命名

确认该文件在目标文件夹中已经被重命名为 instruction 指定的新文件名。

5. 文件删除

确认 instruction 中指定需要删除的文件在 Home 目录中不存在。

--------------------------------
轨迹辅助验证
--------------------------------

如果最终截图无法完全确认文件结构，可以查看执行轨迹中的关键操作，例如：

- 是否执行了文件移动操作
- 是否执行了文件重命名操作
- 是否执行了文件删除操作

轨迹只用于确认关键操作是否发生，不需要逐步评分。

--------------------------------
任务成功判定
--------------------------------

只有当以下所有条件均满足时，任务判定为 1.0：

1. 目标文件夹存在。
2. 指定文件已移动到目标文件夹。
3. 文件已正确重命名。
4. 指定需要保留的文件仍存在。
5. 指定需要删除的文件不存在。

如果任一条件不满足，则任务判定为 0.0。

--------------------------------
输出格式
--------------------------------

请按照以下 JSON 格式输出评估结果：

{
  "reward": 1.0 或 0.0,
  "reason": "逐条简要说明每个检查点是否满足，并说明判断依据（截图或轨迹）"
}

--------------------------------
评估结束
--------------------------------
"""

FILE_TASK_TEMPLATE = """
任务开始前，如果当前有窗口打开，请先关闭所有窗口并回到桌面。

进入 Home 文件夹。

检查是否存在名为 {target_folder} 的文件夹，如果不存在则创建该文件夹。

在 Home 文件夹中检查以下文件是否存在：
{source_file_1}、{source_file_2}。
如果某个文件不存在，则创建该文件。

将 {source_file_1} 移动到 {target_folder} 文件夹中。

进入 {target_folder} 文件夹，将 {source_file_1} 重命名为 {target_file_1}。

返回 Home 文件夹。

如果 Home 文件夹中存在 {delete_file} 文件，则删除该文件。

完成后关闭所有窗口并结束任务。"""
