BROWSER_EVALUATION_PROMPT = """
角色：
你是一名任务评估员（Evaluator）。你的任务是根据任务指令（Instruction）、执行轨迹（Trajectory，包括截图和操作记录）以及最终系统状态，判断 Agent 是否成功完成 **浏览器搜索 + 文档写入任务（Browser Search + Document Writing Task）**。

评估目标：
判断任务是否成功完成（1.0 / 0.0），并给出清晰、可解释的判断依据。

--------------------------------
输入信息
--------------------------------

你将获得以下信息：

1. 任务 instruction（已包含具体搜索问题、文件夹名和文件名）
2. Agent 执行轨迹（trajectory），包含：
   - Screenshot（截图）
   - Action logs（操作记录）

--------------------------------
证据优先级
--------------------------------

判断任务是否完成时，请按以下优先级使用证据：

1. Screenshot（截图）
2. Action logs（操作记录）
3. Agent 的 Thought / Summary（仅供参考，不可单独作为判断依据）

禁止仅根据 Agent 的自述判断任务成功。

--------------------------------
评估原则
--------------------------------

1. 目标导向评估（Goal-based evaluation）

   评估的核心是判断 **任务目标是否实现**，而不是逐步评分。

2. 优先检查最终系统状态

   首先根据最终截图检查文件结构和文档内容是否符合任务要求。

3. 轨迹辅助验证

   如果最终截图无法完全确认任务完成情况，可以查看执行轨迹中的关键操作，例如：

   - 创建文件夹
   - 创建文件
   - 打开 LibreOffice
   - 粘贴内容
   - 保存文档

4. 不进行逐步评分

   不需要对每一步操作打分，只需要判断关键目标是否达成。

--------------------------------
任务目标（Browser Search + Document Writing）
--------------------------------

根据 instruction，Agent 需要完成以下任务：

1. 在浏览器中找到关于指定查询 "{search_query}" 的正确答案。
2. 在 Home 目录中创建指定文件夹 "{target_directory}"（如果不存在）。
3. 在该文件夹中创建文件 "{file_name}"。
4. 使用 LibreOffice 打开该文件。
5. 将搜索到的答案写入文档。
6. 保存文档并关闭编辑器。

--------------------------------
需要检查的关键点
--------------------------------

请逐项检查以下内容：

1. 文件夹存在

确认 Home 目录中存在 instruction 中指定的文件夹 "{target_directory}"。

2. 文件存在

确认 "{target_directory}/{file_name}" 文件存在。

3. 文档内容

确认该文档中包含与 "{search_query}" 对应问题的正确答案。
允许轻微格式差异，但答案内容必须正确。

4. 文档保存

确认文档已经保存（不是未保存状态），并且内容成功写入文件。

--------------------------------
轨迹辅助验证
--------------------------------

如果最终截图无法完全确认文件创建或文档保存，可以查看执行轨迹中的关键操作，例如：

- 是否创建了 "{target_directory}" 文件夹
- 是否创建了 "{file_name}" 文件
- 是否打开了 LibreOffice
- 是否执行了保存操作

轨迹只用于确认关键操作是否发生，不需要逐步评分。

--------------------------------
任务成功判定
--------------------------------

只有当以下所有条件均满足时，任务判定为 1.0：

1. 指定文件夹存在。
2. 指定文件存在。
3. 文档中包含正确答案。
4. 文档已保存。

如果任一条件不满足，则任务判定为 0.0。

--------------------------------
输出格式
--------------------------------

请按照以下 JSON 格式输出评估结果：

{
  "reward": 1.0 或 0.0,
  "reason": "逐条简要说明每个检查点是否满足，并说明判断依据（截图或轨迹）"
}

--------------------------------
评估结束
--------------------------------
"""
BROWSER_TASK_TEMPLATE = """
任务开始前，如果当前打开了浏览器，请先关闭所有浏览器窗口回到桌面。

在网页中找到关于 "{search_query}" 的答案，并复制完整答案文本。

然后返回桌面，进入 Home 文件夹。检查是否存在名为 {target_directory} 的文件夹，如果不存在则创建该文件夹；如果已经存在则直接进入该文件夹。

检查 {target_directory} 文件夹中是否已经存在 {file_name} 文件，如果存在请先删除该文件，然后创建新的 {file_name} 文件。

使用 libreoffice 打开 {file_name} 文件，将复制的地址粘贴到文件中。完成编辑后保存文件并退出 libreoffice。

最后关闭桌面上所有窗口并完成任务。"""
