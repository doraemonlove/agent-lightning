AUDIT_EVALUATION_PROMPT = """
角色：
你是一名任务评估员（Evaluator）。你的任务是根据任务指令（Instruction）以及 Agent 的执行轨迹（Trajectory，包括截图和操作记录），判断 Agent 是否成功完成 **资产盘点审核任务（Asset Audit Task）**。

评估目标：
判断任务是否成功完成（1.0 / 0.0），并给出清晰、可解释的判断依据。

--------------------------------
输入信息
--------------------------------

你将获得以下信息：

1. 任务 instruction（已包含具体盘点计划名称）
2. Agent 执行轨迹（trajectory），包含：
   - Screenshot（截图）
   - Action logs（操作记录）

--------------------------------
证据优先级
--------------------------------

判断任务是否完成时，请按以下优先级使用证据：

1. Screenshot（截图）
2. Action logs（操作记录）
3. Agent 的 Thought / Summary（仅供参考，不可单独作为判断依据）

禁止仅根据 Agent 的自述判断任务成功。

--------------------------------
评估原则
--------------------------------

1. 目标导向评估（Goal-based evaluation）

   评估的核心是判断 **任务目标是否实现**，而不是逐步评分。

2. 优先检查最终状态

   首先根据执行轨迹最后阶段的截图检查系统状态，例如：
   - 是否进入资产审核系统页面
   - 是否完成资产审核

3. 轨迹辅助验证

   如果最终截图无法完全确认任务完成情况，可以查看执行轨迹中的关键操作，例如：

   - 打开资产审核网站
   - 设置筛选条件
   - 点击盘点审核相关按钮
   - 对记录执行通过或驳回

4. 不进行逐步评分

   不需要对每一步操作打分，只需要判断关键任务目标是否达成。

--------------------------------
任务目标（Asset Audit Task）
--------------------------------

根据 instruction，Agent 需要完成以下资产盘点审核任务：

1. 打开资产审核系统网站。
2. 设置筛选条件：
   - 盘点计划 = instruction 中指定的计划名称
   - 盘点审核结果 = 未盘点
3. 在筛选结果中，对 **2 条资产记录** 进行盘点审核。

审核规则：

每条记录会显示系统条码和对应的资产图片。

Agent 需要判断图片中的条码是否与系统条码一致：

- 如果条码完全一致，应选择 **通过**
- 如果条码不一致、无法识别或图片模糊，应选择 **驳回**

--------------------------------
需要检查的关键点
--------------------------------

请逐项检查以下内容：

1. 网站访问

确认 Agent 已成功进入资产审核系统页面。

2. 筛选条件

确认 Agent 设置了正确的筛选条件：

- 盘点计划 = instruction 指定的计划名称
- 盘点审核结果 = 未盘点

3. 审核数量

确认 Agent **成功完成 2 条记录的审核操作**。

4. 审核逻辑

对于每条审核记录：

- 如果图片条码与系统条码一致 → 应选择通过
- 如果条码不一致或无法识别 → 应选择驳回

--------------------------------
轨迹辅助验证
--------------------------------

如果最终截图无法确认审核数量，可以查看执行轨迹确认：

- 是否执行了两次审核操作
- 是否点击了通过或驳回按钮
- 审核窗口是否关闭

轨迹只用于确认关键操作是否发生，不需要逐步评分。

--------------------------------
任务成功判定
--------------------------------

只有当以下所有条件均满足时，任务判定为 1.0：

1. 成功进入资产审核系统页面。
2. 正确设置筛选条件。
3. 成功完成 **2 条记录的审核**。
4. 每条记录的审核判断符合条码规则。

如果任一条件不满足，则任务判定为 0.0。

--------------------------------
输出格式
--------------------------------

请按照以下 JSON 格式输出评估结果：

{
  "reward": 1.0 或 0.0,
  "reason": "逐条简要说明每个检查点是否满足，并说明判断依据（截图或轨迹）"
}

--------------------------------
评估结束
--------------------------------
"""
AUDIT_TASK_TEMPLATE = """
任务开始前，如果当前浏览器已经打开，请先关闭所有浏览器窗口并回到桌面。

随后重新打开浏览器，并访问以下资产审核系统页面：

https://apaas-dev27194.aedev.feishuapp.cn/ae/apps/v3/shunfeng_operating_platform__c/pc/aadie373q5ebw?lane_id=develop

进入系统后，请完成以下资产盘点任务。

首先，在页面中找到资产盘点审核相关界面，并设置筛选条件：

- 盘点计划：{plan_name}
- 盘点审核结果：未盘点

盘点计划的选择列表可能不会同时显示所有计划，盘点计划列表支持滚动或搜索。
完成筛选后，页面将显示待审核的资产记录列表。

每条记录通常包含以下信息：

- 系统记录的资产条码
- 对应资产的图片
- 用于执行审核的操作按钮（例如“盘点审核”、“通过”或“驳回”等）

资产图片支持放大查看，以便更清晰地识别条码内容。

你的任务是对 **2 条资产记录** 进行盘点审核。

审核规则如下：

请检查图片中的条码是否与系统记录的条码一致：

- 如果图片中的条码与系统条码 **完全一致**，选择 **通过**
- 如果条码 **不一致、无法识别或图片不清晰**，选择 **驳回**

完成两条记录的审核后，结束盘点任务。

最后关闭浏览器并结束任务。
"""
